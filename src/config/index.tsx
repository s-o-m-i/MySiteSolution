// Importing Firebase services from the firebase.js file
import { auth, storage, fireStoreDB, realTimeDB, firebase } from "./firebase";

// Exporting Firebase services for use in other parts of the application
export { fireStoreDB, realTimeDB, auth, storage, firebase };
